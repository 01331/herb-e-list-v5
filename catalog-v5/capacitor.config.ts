import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.myapp',
  appName: 'herb-e-list-v5',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
